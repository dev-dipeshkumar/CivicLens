# CivicLens AI — Vercel environment variables
# Import this file via: Vercel Project → Settings → Environment Variables → "Import .env"
#
# ⚠️  Replace the VITE_API_BASE_URL value with your deployed FastAPI backend URL
#     (Render / Railway / Fly.io). No trailing slash.
#     Leave it EMPTY only if you have not deployed the backend yet — the site
#     will build but API calls will fail at runtime.

VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_API_KEY=civiclens-demo-key
